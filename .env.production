# Production Environment Variables
# These are set in Railway dashboard, not from this file

# Database Configuration (Railway sets this automatically)
DATABASE_URL="postgresql://postgres:password@postgres.railway.internal:5432/railway"

# JWT Configuration (Set in Railway Variables)
JWT_SECRET=your-production-jwt-secret-here
JWT_EXPIRES_IN=24h

# Application Configuration
PORT=3009
NODE_ENV=production

# Email Configuration (Set in Railway Variables)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
FROM_EMAIL=noreply@housemajor.com

# Google OAuth (Set in Railway Variables)
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_CALLBACK_URL=https://your-app.railway.app/auth/google/callback

# Cloudinary (Set in Railway Variables)
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret

# Frontend URL (Set in Railway Variables)
FRONTEND_URL=https://your-frontend-url.com